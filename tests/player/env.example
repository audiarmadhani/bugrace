# Copy to .env.player.local — player automation against ShopVerse only

# ShopVerse URL (production or local with APP_MODE=store)
SHOPVERSE_URL=http://127.0.0.1:3000

# Store test account (from challenge page)
STORE_USERNAME=alice
STORE_PASSWORD=Password123
